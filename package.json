{
  "name": "srm-financial-stock-market-insights-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --workspace client & npm run dev --workspace server",
    "install-all": "npm install --workspace client && npm install --workspace server"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
